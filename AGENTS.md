# AGENTS.md

- 中文回答。
- 英文代码注释。
- 默认使用 UTF-8 读写文本文件。
- 在本仓库运行 Python 脚本、测试、notebook 辅助脚本或验证命令时，默认使用 AIMS4PT conda 环境：
  `C:\Users\13493\miniconda3\envs\AIMS4PT\python.exe`
- 不要用 base 环境 `C:\Users\13493\miniconda3\python.exe` 或 Codex bundled Python 做本项目验证，除非用户明确要求。
- 需要通过 conda 调用环境命令时，使用：
  `conda run -n AIMS4PT <command>`
